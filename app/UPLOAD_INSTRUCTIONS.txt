DRONERIS R1.4.0 ADAPTIVE FIRST CUT POOL — COMPLETE BACKEND

Functional change in this revision:
- removes the legacy fixed 7-scene deterministic First Cut candidate pool
- standard 75 s First Cut now generates 15 chronological candidates (~5 s nominal each)
- shorter targets scale adaptively (6-18 candidates)
- multi-source shared-pool selection is also adaptive and no longer capped by 7 editorial roles
- AI Director LOW transition policy is updated; Vision logic, renderer, upload API and frontend contract are unchanged

Expected version from /health:
DRONERIS_RENDER_BACKEND_R1.4.0_ADAPTIVE_FIRST_CUT_POOL_R1_FREE_SAFE

Deployment:
1. Replace the existing backend repository with this complete package.
2. Deploy/redeploy on Render.
3. Check /health and confirm the version above.
4. Run one real ANALYZE/FIRST CUT test before changing Director prompt/pacing rules.
